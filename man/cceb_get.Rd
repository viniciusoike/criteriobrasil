% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cceb-data.R
\name{cceb_get}
\alias{cceb_get}
\title{Get a CCEB table}
\usage{
cceb_get(table = "distribution", edition = NULL)
}
\arguments{
\item{table}{Character scalar. One of \code{"editions"}, \code{"points"},
\code{"cutoffs"}, \code{"income"}, \code{"distribution"}, or \code{"all"}. The default is
\code{"distribution"}.}

\item{edition}{\code{NULL}, a whole-number CCEB edition identifier, or \code{"all"}.
\code{NULL} selects the latest edition available for each requested table.}
}
\value{
A tibble for one table. When \code{table = "all"}, a named list of five
tibbles in the order returned by \code{\link[=cceb_list_tables]{cceb_list_tables()}}.
}
\description{
Downloads a clean CCEB table from the package's GitHub release assets.
Repeated requests for the same table in one R session use an in-memory copy.
}
\details{
\code{edition} identifies a published CCEB edition, not necessarily the year of
the underlying survey or income estimate. Use \code{table = "editions"} to
inspect effective dates and reference years.

The function returns the released data without renaming, reordering, or
transforming columns. It only filters rows by \code{edition_id}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
cceb_get()
cceb_get("income", edition = 2024)
cceb_get("points", edition = "all")
\dontshow{\}) # examplesIf}
}
